CREATE FUNCTION dbo.GetCurrentUserID()
RETURNS INT
AS
BEGIN
    DECLARE @CurrentUserID INT;

    SELECT @CurrentUserID = PersonID
    FROM Users
    WHERE Username = SUSER_SNAME();

    RETURN @CurrentUserID;
END;

SELECT dbo.GetCurrentUserID() AS CurrentUserID;
